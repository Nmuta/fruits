<%= form_with(model: fruit, local: true) do |form| %>
  <% if fruit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fruit.errors.count, "error") %> prohibited this fruit from being saved:</h2>

      <ul>
      <% fruit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :fruit_name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :fruit_url %>
  </div>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_field :source, id: :fruit_source %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :fruit_price %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :fruit_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
